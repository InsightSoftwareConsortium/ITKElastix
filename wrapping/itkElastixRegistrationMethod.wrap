itk_wrap_class("itk::ElastixRegistrationMethod" POINTER)
  UNIQUE(dims "${ITK_WRAP_IMAGE_DIMS};4")
  foreach(d ${dims})
    foreach(t1 ${WRAP_ITK_SCALAR})
      foreach(t2 ${WRAP_ITK_USIGN_INT} ${WRAP_ITK_SIGN_INT})
        itk_wrap_template("${ITKM_I${t1}${d}}${ITKM_I${t2}${d}}"  "${ITKT_I${t1}${d}},${ITKT_I${t2}${d}}")
      endforeach()
    endforeach()
  endforeach()
itk_end_wrap_class()
